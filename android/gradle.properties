# Version of the published io.github.hbmartin:mosh-android artifact.
# Independent of the embedded mosh version (see MOSH_VERSION in
# lib/build.gradle.kts); must match the android-jni-v* release tag.
VERSION_NAME=0.1.0

org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
android.useAndroidX=true
